<div class="chats__container">
    <div class="chatsList">
        <div class="profileLink"><a href="{{profileLink.url}}">{{profileLink.text}}</a></div>
        <input class="search" placeholder="Поиск"></input>
        <div class="chatsList">
            {{#each chatsList__items}}
            <div class="chatsList__item">
                <div>
                    <img src={{this.img}} alt={{this.title}} class="chat__avatar">
                </div>
                <div>
                    <p class='chat__title'>{{this.title}}</p>
                    <p class='chat__text'>{{this.text}}</p>
                </div>
            </div>
            {{/each}}
        </div>
    </div>
    <div class="currentChat">
        <div class="currentChat__header">
            <img src={{companion__avatar}} alt="Ваш собеседник" class="companion__avatar">
            <p>Вадим</p>
            <div class="extra"><img src={{dots}} alt="" class="dots">
            </div>
        </div>
        <div class="currentChat__main">
            <div class="currentChat__main-date">{{date}}</div>
            {{#each currentChat__main_messages}}
            <div class="currentChat__main_messages chat__text {{this.author}}">
                <div class="currentChat__message {{this.author}}">
                    {{#if this.image}}
                    <img src={{this.content}} alt="фото" class="message_img">
                    {{else}}
                    {{{this.content}}}
                    {{/if}}
                </div>
            </div>
            {{/each}}
        </div>
        <div class="currentChat__sendMessage">
            <img src={{clip__img}} alt="Отправить файл" class="clip">
            {{{input}}}
            {{{button}}}
        </div>
    </div>
</div>